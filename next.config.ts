import type { NextConfig } from "next";
// const withPWA = require('next-pwa')({
//   dest: 'public', // Service Worker 的输出目录
//   disable: process.env.NODE_ENV === 'development', // 开发环境禁用，避免缓存干扰
//   register: true, // 注册 Service Worker
//   skipWaiting: true, // 立即开始激活新的 SW
// });
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
